# Demo training configuration for the portfolio version of the MP-GCN adaptation.
# This file is intentionally lightweight. It supports the runnable demo in main.py.
dataset_root_folder: data/raw/playground_dataset
out_folder: data/playground

model:
  name: MP_GCN
  num_classes: 2

training:
  batch_size: 2
  epochs: 1
  mode: demo
